/**
 * Opaque identifier for a conversation inside the message channel domain.
 */
export type ConversationId = string;

/**
 * Opaque identifier for an individual message inside the message channel domain.
 */
export type MessageId = string;
